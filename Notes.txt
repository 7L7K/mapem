Notes


how to  delte table:

TRUNCATE TABLE individuals, families, events, locations, residence_history, tree_versions, tree_people, tree_relationships RESTART IDENTITY CASCADE;


How to run project rom root -> ./run_all.sh 


how to start env -> source .venv/bin/activate


how to run databade -> psql genealogy_db


Deleting .vite cache -> rm -rf node_modules/.vite


✅ 1. Clear the cache
Just to be safe, wipe the Vite dev server’s cache and restart:
rm -rf node_modules/.vite
npm run dev

🧼 Bonus Tip: Track Geocoding Progress
Run:
SELECT COUNT(*) FROM locations;
SELECT COUNT(*) FROM locations WHERE latitude IS NOT NULL;
This helps track how many still need geocoding.
